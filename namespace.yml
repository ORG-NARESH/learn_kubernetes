apiVersion: v1
kind: Pod
metadata:
contexts:
- context:
    namespace: dev_expense_ns
  name: nginx-dev
spec:
  containers:
  - name: nginx-perl-container
    image: nginx:perl
    ports:
    - containerPort: 80
